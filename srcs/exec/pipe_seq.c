#include "../../includes/nsh.h"

int exec_pipe_seq(t_sh *nsh, t_cmd *cmd)
{
    int     i;
    int     red[2];
    int     origin[2];

    i = 0;
    // while (i < cmd->smpnb)
    // {

    //     i++;
    // }
    (void)nsh;
    (void)cmd;
    return (0);
}